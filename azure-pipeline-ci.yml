trigger: none

pr:
  branches:
    include:
      - '*'

stages:
  - stage: CI_Checks
    jobs:
      - job:
      displayName: print statement
      pool:
        vmImage: 'ubuntu-latest'
    steps:
      - script: |
        echo "Hello World"
        displayName: 'Hello World'
